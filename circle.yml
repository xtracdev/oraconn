machine:
  services:
   - docker

dependencies:
  override:
    - docker run --rm -v "$PWD":/go/src/github.com/xtracdev/oraconn -w /go/src/github.com/xtracdev/oraconn -e FEED_DB_USER=$FEED_DB_USER -e FEED_DB_PASSWORD=$FEED_DB_PASSWORD -e FEED_DB_HOST=$FEED_DB_HOST -e FEED_DB_PORT=$FEED_DB_PORT -e FEED_DB_SVC=$FEED_DB_SVC  xtracdev/goora bash -c make

test:
  override:
    - echo 'test automation tbd'